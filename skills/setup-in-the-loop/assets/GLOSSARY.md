# Glossary

Canonical project terms. Code mirrors these — identifiers and API fields use the canonical word.
Changes only when a **term** is added or redefined. Define what a term *is*, not what it does.
Project-specific concepts only — no general programming vocabulary.

**<Term>**:
One or two sentences. What it IS, not what it does.
_Avoid_: <synonym to not use>, <another>
